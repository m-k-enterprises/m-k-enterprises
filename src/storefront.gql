{
  shop {
    id
    name
    shipsToCountries
    primaryDomain {
      url
    }
    brand {
      slogan
      shortDescription
      logo {
        image {
          altText
          originalUrl: url
          displayUrl: url(transform: {maxWidth: 2880})
          width
          height
        }
      }
      # Use `squareLogo` for square-aspect-ratio contexts (e.g., avatars, app icons, profile images).
      # Use `logo` for rectangular or flexible-aspect contexts (e.g., headers, navigation bars, banners) and as the default elsewhere.
      squareLogo {
        image {
          altText
          originalUrl: url
          displayUrl: url(transform: {maxWidth: 2880})
          width
          height
        }
      }
      colors {
        primary {
          background
          foreground
        }
      }
      coverImage {
        image {
          altText
          originalUrl: url
          heroUrl: url(transform: {crop: CENTER, maxWidth: 5760, maxHeight: 3240})
          width
          height
        }
      }
    }
  }
  articles(first: 6, sortKey: PUBLISHED_AT, reverse: true) {
    nodes {
      id
      title
      onlineStoreUrl
      handle
      excerpt
      excerptHtml
      image {
        altText
        originalUrl: url
        newsImageUrl: url(transform: {crop: CENTER, maxWidth: 1920, maxHeight: 1080})
        width
        height
      }
      publishedAt
    }
  }
}
